name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]  # Set this to your default branch
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'  # Set this to your project's Node.js version
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          BASE_PATH: '/${{ github.repository }}'  # Adds the repository name as a base path

      - name: Configure Pages
        uses: actions/configure-pages@v3

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v2
        with:
          path: ./dist  # Adjust if your build output directory is different

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
